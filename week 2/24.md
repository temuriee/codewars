# ამოცანა

Welcome. In this kata, you are asked to square every digit of a number and concatenate them.

For example, if we run 9119 through the function, 811181 will come out, because 92 is 81 and 12 is 1. (81-1-1-81)

Example #2: An input of 765 will/should return 493625 because 72 is 49, 62 is 36, and 52 is 25. (49-36-25)

Note: The function accepts an integer and returns an integer.

Happy Coding!

---

# ამოხსნა

export class Kata {
static squareDigits(num: number): number {

return +num.toString().split("").map((nums) => String(Number(nums) \*\* 2)).join("")

}
}

---

# how it works

first of all we use function called toString() so we can work in strings and add numbers to eachother not like mathway but string way so in short to glue them together and not to add up then we use function called split so we get only 1 digits then with map we give it argument called nums which contains each digit one by one then we turn that argument nums into number and square it and then again with String() we turn it into string and then join them together so if our number at the beggining was 123 our number now will be 1 ** 2 2 ** 2 3 \*\* 3 and that would give us = 149 but in string so at the beggining we use symbol + to make it back to number again
