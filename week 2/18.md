# ამოცანა

```javascript
Implement a function which convert the given boolean value into its string representation.

Note: Only valid inputs will be given.
```

---

# ამოხნსა

```javascript

export const booleanToString = (b:boolean):string => {
  return b ? "true" : "false"
};
```

## How It Works

we are checking if argument b is true or false if it is true it will return true as a string and if its false it will return false as a string
